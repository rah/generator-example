package org.tohu.generator;
 /*
  * This package has been generated using Tohu Generator
  * Do not modify by hand if you wish to regenerate
  */
import org.tohu.Question;
import org.tohu.Group;
import org.tohu.Note;

rule "Create_Note_Note-1_30"
dialect "mvel"
	when
		$q : Question( id == "<h2>Car not listed?</h2> Click here" )
		$g : Group (items != null && items contains "<h2>Car not listed?</h2> Click here")
		not ( Note( id == "Note-1" ) )
	then
		Note note = new Note("Note-1", "Transmission");
		note.setPresentationStyles({"expandedTip"});
		insert (note);		
end
rule "Insert_Tip_Note-1_30"
dialect "mvel"
	when
		$q : Question( id == "<h2>Car not listed?</h2> Click here" )
		$g : Group (items != null && items contains "<h2>Car not listed?</h2> Click here" && items not contains "Note-1")
		$n : Note( id == "Note-1", (presentationStyles != null && presentationStyles contains "tip") )
	then
		$g.insertItem($n.getId(), $q.getId());
		update ($g);
end
rule "Insert_Expanded_Tip_Note-1_30"
dialect "mvel"
	when
		$q : Question( id == "<h2>Car not listed?</h2> Click here" )
		$g : Group (items != null && items contains "<h2>Car not listed?</h2> Click here" && items not contains "Note-1")
		$n : Note( id == "Note-1", (presentationStyles != null && presentationStyles contains "expandedTip") )
	then
		$g.appendItem($n.getId(), $q.getId());
		update ($g);
end


