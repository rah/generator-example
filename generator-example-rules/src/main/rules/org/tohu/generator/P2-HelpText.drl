package org.tohu.generator;
 /*
  * This package has been generated using Tohu Generator
  * Do not modify by hand if you wish to regenerate
  */
import org.tohu.Question;
import org.tohu.Group;
import org.tohu.Note;

rule "Create_Note_Mods-help_46"
dialect "mvel"
	when
		$q : Question( id == "Winch" )
		$g : Group (items != null && items contains "Winch")
		not ( Note( id == "Mods-help" ) )
	then
		Note note = new Note("Mods-help", "<h2>Modifications</h2><p>If your car has been modified in any way, we cannot insure you.</p><p>None of the extras listed above are modifications.</p>");
		note.setPresentationStyles({"expandedTip"});
		insert (note);		
end
rule "Insert_Tip_Mods-help_46"
dialect "mvel"
	when
		$q : Question( id == "Winch" )
		$g : Group (items != null && items contains "Winch" && items not contains "Mods-help")
		$n : Note( id == "Mods-help", (presentationStyles != null && presentationStyles contains "tip") )
	then
		$g.insertItem($n.getId(), $q.getId());
		update ($g);
end
rule "Insert_Expanded_Tip_Mods-help_46"
dialect "mvel"
	when
		$q : Question( id == "Winch" )
		$g : Group (items != null && items contains "Winch" && items not contains "Mods-help")
		$n : Note( id == "Mods-help", (presentationStyles != null && presentationStyles contains "expandedTip") )
	then
		$g.appendItem($n.getId(), $q.getId());
		update ($g);
end


